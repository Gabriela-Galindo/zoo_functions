const data = require('../data/zoo_data');

function countAnimals(animal) {
//   if (animal === undefined) {
//     const chave = data.species.map((species) => species.name);
//     const valor = data.species.map((species) => species.residents.length);
//     return
//   }
}

// console.log(countAnimals());

module.exports = countAnimals;
